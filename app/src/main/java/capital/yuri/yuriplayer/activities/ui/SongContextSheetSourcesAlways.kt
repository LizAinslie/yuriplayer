package capital.yuri.yuriplayer.activities.ui

/**
 * NOTE: The real fix lives in [SongContextSheet] inside MediaActionSheet.kt:
 * Sources menu item is shown for **every** song, not only multi-source.
 * Multi-source **badge** remains gated on count > 1.
 *
 * This file is intentionally empty of UI — kept as a marker commit companion.
 * Implementation is inlined in MediaActionSheet + SwipeAddSongRow.
 */
